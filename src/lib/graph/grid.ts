/**
 * @file grid.ts -- Hard grid dimensions for the Graph 2.0 Slot-Editor.
 *
 * Lives outside `$lib/server` so the constants can be imported from
 * client-side `.svelte` files without leaking server-only code.
 */

export const GRAPH_GRID_COLS = 20;
export const GRAPH_GRID_ROWS = 10;
