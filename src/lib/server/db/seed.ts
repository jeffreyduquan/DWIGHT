/**
 * @file seed.ts — intentionally empty.
 *
 * DWIGHT does NOT seed any data. Users register themselves and build their
 * own Modes via /modes. This file is kept so the npm script slot stays valid
 * but is a no-op.
 *
 * Run via:  pnpm exec tsx src/lib/server/db/seed.ts
 */
console.log('Seed: nothing to do — DWIGHT seeds no data.');
process.exit(0);
